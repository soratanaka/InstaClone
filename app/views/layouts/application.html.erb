<!DOCTYPE html>
<html>
  <head>
    <title>InstaClone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
  <header class="header clearfix">
    <% if logged_in? %>
      <ul class="top_menu clearfix">
        <li><%= link_to "マイページ", user_path(current_user.id) ,class:'top_list' %></li>
        <li><%= link_to "ログアウト", session_path(current_user.id), method: :delete ,class:'top_list' %></li>
        <li><%= link_to "新しい投稿", new_picture_path(current_user.id) ,class:'top_list' %></li>
        <li><%= link_to "投稿一覧", pictures_path(current_user.id) ,class:'top_list' %></li>
        <li><%= link_to 'お問合せ', new_contact_path ,class:'top_list' %></li>
      </ul>
    <% else %>
      <ul class="top_menu clearfix">
        <li><%= link_to "Sign up", new_user_path ,class:'top_list' %></li>
        <li><%= link_to "Login", new_session_path ,class:'top_list' %></li>
        <li><%= link_to 'お問合せ', new_contact_path ,class:'top_list' %></li>
      </ul>
    <% end %>
  </header>
  <div class="text-center">
    <%= yield %>
  </div>
  <footer class="footer">
    <p>Make Sora Tanaka from Dive InTo Code</p>
  </footer>
  </body>
</html>
</html>
